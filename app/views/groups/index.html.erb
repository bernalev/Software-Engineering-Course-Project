<h1>All Groups</h1>

<table>
  <tr>
    <th>Group Name</th>
    <th>Course ID</th>
  </tr>
 
  <% @groups.each do |group| %>
    <tr>
      <td><%= link_to group.name, group_path(group) %></td>
      <td>Test Data</td>
    </tr>
  <% end %>
  <tr>
  	<td><%= link_to 'New Group', new_group_path() %></td>
  	<td></td>
  </tr>
</table>