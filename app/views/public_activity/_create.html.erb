<li class="list-group-item text-center">
      <% if a.trackable %>
	      <%= a.trackable.title %> was posted.
      <% else %>
	    A posting that is currently deleted was added.
      <% end %>
</li>
