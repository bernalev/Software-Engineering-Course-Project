<h1>All Notes</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th>Student</th>
  </tr>
 
  <% @notes.each do |note| %>
    <tr>
      <td><%= note.title %></td>
      <td><%= note.body %></td>
      <td><%= "#{note.student.first_name} #{note.student.last_name}" %></td>
    </tr>
  <% end %>
</table>